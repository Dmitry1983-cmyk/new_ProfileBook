<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="newProfileBook.View.SettingsPage"
             xmlns:resources="clr-namespace:newProfileBook.RESX"
             Title="{Binding Resources[SettingsPage]}"
             Style="{StaticResource PageStyle}"
             >

    <ContentPage.ToolbarItems>
        <ToolbarItem
            Order="Primary"
            Priority="1"
            Command="{Binding LogOutTappedCommand}"
            IconImageSource="ic_exit_to_app.png"/>
    </ContentPage.ToolbarItems>

    <ContentPage.Content>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="50"/>
                <RowDefinition Height="50"/>
                <RowDefinition Height="50"/>
                <RowDefinition Height="50"/>
                <RowDefinition Height="50"/>
                <RowDefinition Height="50"/>
                <RowDefinition Height="50"/>
                <RowDefinition Height="50"/>
            </Grid.RowDefinitions>


            <Label
                Grid.Row="0"
                Text="{Binding Resources[SortProfiles]}"
                VerticalOptions="Center"
                HorizontalOptions="Center"
                Style="{StaticResource LabelStyle}"/>
            <RadioButton 
                Grid.Column="0" 
                Grid.Row="1"
                Margin="20,0,0,0"
                IsChecked="{Binding SortByName}"/>

            <Label 
                Grid.Row="1"
                Text="{Binding Resources[SortbyName]}"
                VerticalOptions="Center"
                Margin="70,0,0,0"
                Style="{StaticResource LabelStyle}"/>

            <RadioButton
                Grid.Column="0" 
                Grid.Row="2"
                Margin="20,0,0,0"
                IsChecked="{Binding SortByNickName}"/>
            <Label 
                Grid.Row="2"
                Text="{Binding Resources[SortbyNickname]}"
                VerticalOptions="Center"
                Margin="70,0,0,0"
                Style="{StaticResource LabelStyle}"/>

            <RadioButton 
                Grid.Column="0" 
                Grid.Row="3"
                Margin="20,0,0,0"
                IsChecked="{Binding SortByDateTime}"/>

            <Label 
                
                Grid.Row="3"
                Text="{Binding Resources[SortbyDate]}"
                VerticalOptions="Center"
                Margin="70,0,0,0"
                Style="{StaticResource LabelStyle}"/>

            <Label 
                Grid.Row="4"
                Text="{Binding Resources[Theme]}"
                VerticalOptions="Center"
                HorizontalOptions="Center"
                Style="{StaticResource LabelStyle}"/>
            <CheckBox
                Grid.Row="5"
                Margin="20,0,0,0"
                IsChecked="{Binding CheckTemeDarkLight}"/>
            <Label 
                Grid.Row="5"
                Text="{Binding Resources[DayNightTheme]}"
                VerticalOptions="Center"
                Margin="70,0,0,0"
                Style="{StaticResource LabelStyle}"/>

            <Label 
                Grid.Row="6"
                Text="{Binding Resources[Language]}"
                VerticalOptions="Center"
                HorizontalOptions="Center"
                Style="{StaticResource LabelStyle}"/>

            <Picker
                Grid.Row="7"
                Margin="20,0,0,0"
                Title="{Binding Resources[Language]}"
                ItemsSource="{Binding Languages}"
                SelectedItem="{Binding CheckLanguageEngRus}"/>
            
            <Label 
                Grid.Row="7"
                Text="{Binding Resources[RuEng]}"
                VerticalOptions="Center"
                HorizontalOptions="End"
                Style="{StaticResource LabelStyle}"/>
        </Grid>
    </ContentPage.Content>
</ContentPage>